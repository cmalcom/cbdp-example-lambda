{
    "name": "example-lambda",
    "version": "0.1.0",

    "scripts": {
      "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
      "package": "zip -r package.zip *"
    },
    "dependencies": {
    }
  }
  